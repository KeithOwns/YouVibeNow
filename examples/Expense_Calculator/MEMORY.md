# Dynamic Constraints & Memory
Use this file as your long-term local storage memory. Log learned environment nuances, OS-specific constraints, and user preferences here.

## Learned Preferences
- *[Example]* The user prefers using PowerShell commands over Git Bash.
- *[Example]* Always output processed data into the `Outputs/` folder, never the root directory.

## Decision Log
- **[Date]:** Initialized the local file storage workspace.
