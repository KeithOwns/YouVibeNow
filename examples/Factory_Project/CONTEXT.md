We are building a Python Password Generator script.
Sandbox Constraint: All source code must be saved exclusively to the `src/` directory. All tests must be saved exclusively to the `tests/` directory and run from there.
